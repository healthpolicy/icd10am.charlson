% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify_charlson.R
\name{classify_charlson}
\alias{classify_charlson}
\title{Classify episodes using the Charlson Comorbidity Index}
\usage{
classify_charlson(
  df,
  id,
  format = c("long", "wide"),
  df_diag = NULL,
  diag = "diag",
  diagno = "diagno",
  diag_prefix = "diag",
  method = c("Quan", "Sundararajan")
)
}
\arguments{
\item{df}{A data frame with one row per episode.}

\item{id}{The episode identifier column (unquoted name).}

\item{format}{Input format: \code{"long"} (default) or \code{"wide"}.}

\item{df_diag}{Long-format diagnosis data frame with one row per diagnosis.
Required when \code{format = "long"}. Must contain the episode ID column, a
diagnosis code column, and a diagnosis sequence number column.}

\item{diag}{Name of the diagnosis code column in \code{df_diag} (quoted string).
Default: \code{"diag"}. Used only when \code{format = "long"}.}

\item{diagno}{Name of the diagnosis sequence number column in \code{df_diag}
(quoted string). Default: \code{"diagno"}. Used only when \code{format = "long"}.}

\item{diag_prefix}{Character prefix shared by all diagnosis columns in \code{df}
(e.g. \code{"diag"} matches \code{diag1}, \code{diag2}, ...). Default: \code{"diag"}. Used
only when \code{format = "wide"}.}

\item{method}{Scoring method. \code{"Quan"} (default) applies the Quan et al.
(2005) updated weights; \code{"Sundararajan"} applies the original weights.}
}
\value{
The input \code{df} with additional columns appended:
\describe{
\item{\code{charlson_score}}{Total Charlson comorbidity score (numeric).}
\item{\code{S01} ... \code{S17}}{Binary flags (0/1) for each Charlson comorbidity
category.}
}
Episodes with no matching diagnoses receive a score of 0.
}
\description{
Applies the Charlson Comorbidity Index to admitted patient episodes coded
with ICD-10-AM. Each episode receives a total Charlson score and binary
flags for each of the 17 Charlson comorbidity categories.

Both wide-format (one row per episode, diagnoses in separate columns) and
long-format (separate diagnosis data frame) inputs are supported via the
\code{format} argument.
}
\examples{
dfbase <- icd10am.utils::dfbase
dfdiag <- icd10am.utils::dfdiag

# Long format
classify_charlson(
  df      = dfbase,
  id      = recordID,
  format  = "long",
  df_diag = dfdiag,
  diag    = "diag",
  diagno  = "diagno",
  method  = "Quan"
)

# Wide format
dfwide <- icd10am.utils::dfwide
classify_charlson(
  df          = dfwide,
  id          = recordID,
  format      = "wide",
  diag_prefix = "diag",
  method      = "Quan"
)
}
\references{
Charlson ME, Pompei P, Ales KL, MacKenzie CR (1987). A new method of
classifying prognostic comorbidity in longitudinal studies: development and
validation. \emph{Journal of Chronic Diseases}, 40(5), 373--383.
\doi{10.1016/0021-9681(87)90171-8}

Quan H, Sundararajan V, Halfon P, et al. (2005). Coding algorithms for
defining comorbidities in ICD-9-CM and ICD-10 administrative data.
\emph{Medical Care}, 43(11), 1130--1139.
\doi{10.1097/01.mlr.0000182534.19832.83}
}
